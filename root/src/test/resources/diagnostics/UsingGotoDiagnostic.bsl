Процедура БезПерейти()

    // Тут код

КонецПроцедуры

Процедура СПерейти()

    Перейти ~а;
    // Тут код
    ~а: // Вот такой маневр

КонецПроцедуры

Процедура РеализацияЦиклаСПерейти()

    Сч = 0;
    ~Петля: Сообщить(СтрШаблон("Сч = %1", Сч));
    Сч = Сч + 1;
    
    Если Сч < 10 Тогда

        Перейти ~Петля;

    КонецЕсли;

КонецПроцедуры

Процедура ПравильныйЦикл()

    Для Сч = 0 По 10 Цикл

        Сообщить(СтрШаблон("Сч = %1", Сч))

    КонецЦикла;

КонецПроцедуры
