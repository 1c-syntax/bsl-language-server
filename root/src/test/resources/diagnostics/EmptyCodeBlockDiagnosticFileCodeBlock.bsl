// Проверим что на пустом теле модуля(тот что после методов) не стреляет

Процедура Пустая()
    // Ошибка на весь следующий блок ЕСЛИ. Если КОММЕНТАРИЙ = КОД, то ошибки нет
    Если А Тогда // Сработка

    КонецЕсли;
КонецПроцедуры

Функция ЗапрещенныеСимволыВПути()
	Возврат ЗапрещенныеСимволы;
КонецФункции // Нету
                                    // <-- Ошибки быть должно
