Функция А2(Ч)
    Н = (450 + Ч)/(Ч + 50);
    Сообщить("" + Ч + "->" + Н);
    Возврат Н;
КонецФункции

Функция А(Ч)
    Н = (450 - Ч)/(Ч + 50);
    Сообщить("" + Ч + "->" + Н);
    Возврат Н;
КонецФункции

Процедура Рсчет()

    Ч = 40;
    Р = А2(Ч);    
    Пока Р <> Цел(Р) ИЛИ Р > 5 Цикл
        Ч = Ч + 1;
        Р = А2(Ч);
    КонецЦикла;
    
КонецПроцедуры
Рсчет();