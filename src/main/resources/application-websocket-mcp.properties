# MCP over Streamable HTTP next to LSP over WebSocket (profile `websocket-mcp`, activated by `websocket --mcp`).
# stdout is free here, so fallback logs stay on System.out (see logback-spring.xml).
spring.ai.mcp.server.stdio=false
spring.ai.mcp.server.protocol=STREAMABLE
