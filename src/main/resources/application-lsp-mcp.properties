# MCP over Streamable HTTP next to LSP over stdio (profile `lsp-mcp`, activated by `lsp --mcp`).
# stdout is the LSP protocol channel here, so fallback logs go to System.err (see logback-spring.xml).
spring.ai.mcp.server.stdio=false
spring.ai.mcp.server.protocol=STREAMABLE
