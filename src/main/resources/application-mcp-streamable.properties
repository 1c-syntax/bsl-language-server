# MCP over Streamable HTTP (profile `mcp-streamable`, activated by `mcp --protocol streamable`).
# HTTP-транспорт: stdout свободен, fallback-логи остаются на System.out. Эндпоинт по умолчанию /mcp.
spring.ai.mcp.server.stdio=false
spring.ai.mcp.server.protocol=STREAMABLE
