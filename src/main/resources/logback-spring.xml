<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <!-- Цель fallback-логов: по умолчанию System.out; когда stdout занят протоколом
         (stdio-режимы MCP или LSP с MCP), лаунчер выставляет системное свойство
         CONSOLE_TARGET=System.err (см. BSLLSPLauncher#configureConsoleTarget). -->
    <include resource="language-client-aware-appender.xml" />
    <root level="INFO">
        <appender-ref ref="LANGUAGE_CLIENT_AWARE" />
    </root>
</configuration>
