# MCP over SSE (profile `mcp-sse`, activated by `mcp --protocol sse`).
# HTTP-транспорт: stdout свободен, fallback-логи остаются на System.out.
spring.ai.mcp.server.stdio=false
spring.ai.mcp.server.protocol=SSE
