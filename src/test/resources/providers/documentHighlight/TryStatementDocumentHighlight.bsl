// Тестовый файл для проверки подсветки try-конструкций

Процедура ТестПопытка()
    Попытка
        ВызватьОшибку();
    Исключение
        Сообщить("Ошибка");
    КонецПопытки;
КонецПроцедуры

Процедура ТестНеПопытка()
    Если Истина Тогда
        Сообщить("Не попытка");
    КонецЕсли;
КонецПроцедуры

