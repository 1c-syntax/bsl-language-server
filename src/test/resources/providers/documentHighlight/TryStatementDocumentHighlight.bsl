// Тестовый файл для проверки подсветки try-конструкций

Процедура ТестПопытка()
    Попытка
        ВызватьОшибку();
    Исключение
        Сообщить("Ошибка");
    КонецПопытки;
КонецПроцедуры

Процедура ТестНеПопытка()
    Если Истина Тогда
        Сообщить("Не попытка");
    КонецЕсли;
КонецПроцедуры

// Тест вложенных try-конструкций
Процедура ТестВложенныеПопытки()
    Попытка
        Попытка
            ВызватьОшибку();
        Исключение
            Сообщить("Вложенная ошибка");
        КонецПопытки;
    Исключение
        Сообщить("Внешняя ошибка");
    КонецПопытки;
КонецПроцедуры

// Тест попытки внутри if
Процедура ТестПопыткаВИф()
    Если Истина Тогда
        Попытка
            ВызватьОшибку();
        Исключение
            Сообщить("Ошибка в if");
        КонецПопытки;
    КонецЕсли;
КонецПроцедуры

