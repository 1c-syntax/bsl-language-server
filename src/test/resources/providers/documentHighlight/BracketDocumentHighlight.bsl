// Тестовый файл для проверки подсветки скобок

Функция ТестСкобки()
    Массив = Новый Массив;
    Массив.Добавить((1 + 2) * 3);
    Возврат Массив[0];
КонецФункции

Процедура НеСкобки()
    Если Истина Тогда
        Сообщить("Не скобки");
    КонецЕсли;
КонецПроцедуры

