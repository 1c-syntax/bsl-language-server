#Use  lib-name
// Комментарий
Процедура ИмяПроцедуры(Парам=0)
    Если Истина = Истина Тогда

    // Комментарий
    Возврат;
    КонецЕсли;
    А = (А ИЛИ А);

    А = А
        И (А)
        И А = А
        ИЛИ А;

    А = """";

Пока Истина Цикл
     А=А-1;
КонецЦикла;

КонецПроцедуры

Процедура Другая()
    А = 0;
КонецПроцедуры
