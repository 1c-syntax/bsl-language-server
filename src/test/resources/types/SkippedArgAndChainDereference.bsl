Процедура Тест()
	// Пропущенный довод — Сообщить("X", , "Y") — supplier должен корректно
	// различать индексы аргументов.
	Сообщить("X", , "Y");

	// Сложная цепочка с dereference после methodCall — для findDereferenceForTerminal.
	ТЗ = Новый ТаблицаЗначений;
	ТЗ.Колонки.Добавить("Имя");
	Первая = ТЗ[0].Имя;

	// Цепочка через 2 dereference + methodCall в конце.
	Стр = Новый Структура;
	Стр.Вставить("Поле", Новый Массив);
	Массив2 = Стр.Поле;
	Массив2.Добавить("X");
КонецПроцедуры
