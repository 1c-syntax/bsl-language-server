Перем Кэш;
Перем Настройки Экспорт;
Перем ПередВызовом;
Перем Промежуточный;

Процедура ЗаполнитьКэш()

	Кэш = Новый Соответствие;
	ПослеПрисваивания = Кэш;

КонецПроцедуры

Процедура ОчиститьКэш()

	Кэш = Новый Массив;

КонецПроцедуры

Процедура ИспользоватьПослеВызова()

	Кэш = Новый Соответствие;
	ОчиститьКэш();
	ПослеВызова = Кэш;

КонецПроцедуры

Процедура ДоПервогоПрисваивания()

	ДоПрисваивания = Кэш;
	Кэш = Новый Соответствие;

КонецПроцедуры

Процедура ЗаполнитьНастройки()

	Настройки = Новый Структура;
	Настройки.Вставить("Режим", "Тест");
	ПослеВставки = Настройки;

КонецПроцедуры

Процедура СброситьНастройки()

	Настройки = Новый ТаблицаЗначений;

КонецПроцедуры

Процедура ЗаполнитьПередВызовом()

	ПередВызовом = Новый Структура;
	ОбработатьПередВызовом();

КонецПроцедуры

Процедура ОбработатьПередВызовом()

	ВЧужомТеле = ПередВызовом;

КонецПроцедуры

Процедура ПерезаписатьБезВызова()

	Промежуточный = Новый Массив;
	Промежуточный = Новый Соответствие;

КонецПроцедуры

Процедура ПрочитатьПромежуточный()

	ВДругомТеле = Промежуточный;

КонецПроцедуры
