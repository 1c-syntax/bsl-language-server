Процедура Тест()
	// Унарные операторы.
	А = -10;
	Б = +5;
	В = НЕ Истина;
	Г = -(-7);
	Д = НЕ НЕ Ложь;

	// Бинарные арифметические.
	Е = 10 + 5;
	Ж = 10 - 5;
	З = 10 * 5;
	И1 = 10 / 5;
	К = 10 % 3;

	// Бинарные сравнения.
	Л = 10 > 5;
	М = 10 >= 5;
	Н = 10 < 5;
	О = 10 <= 5;
	П = 10 = 5;
	Р = 10 <> 5;

	// Логические.
	С = Истина И Ложь;
	Т = Истина ИЛИ Ложь;
	У1 = НЕ (Истина И Ложь);

	// Тернарный.
	Ф = ?(Истина, 1, 2);
	Х = ?(10 > 5, "большое", "малое");
	Ц = ?(Ложь, ?(Истина, 1, 2), 3);

	// Конкатенация строк.
	Ч = "a" + "b";
	Ш = "x" + 10;
	Щ = 5 + " лет";

	// Цепочки бинарных.
	Ы = 1 + 2 + 3 + 4 + 5;
	Ъ = 100 - 50 + 25;
	Ь = 2 * 3 + 1;

	// Скобки.
	Э = (1 + 2) * 3;
	Ю = ((1 + 2) * 3) / 2;

	// Тип с условным выражением.
	Я = ?(А > 0, "плюс", "минус") + "_суффикс";

	// Длинная цепочка точечного доступа.
	Стр = Новый Структура("X, Y", Новый Структура("Внутр", 42), 100);
	Глуб = Стр.X.Внутр;
КонецПроцедуры
