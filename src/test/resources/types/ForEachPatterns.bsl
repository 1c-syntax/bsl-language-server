Процедура Тест()
	// Цикл по массиву строк.
	Строки = Новый Массив;
	Строки.Добавить("один");
	Строки.Добавить("два");
	Для Каждого Стр Из Строки Цикл
		ДлинаСтр = СтрДлина(Стр);
	КонецЦикла;

	// Цикл по массиву чисел.
	Числа = Новый Массив;
	Числа.Добавить(1);
	Числа.Добавить(2);
	Для Каждого Чис Из Числа Цикл
		Удвоенное = Чис * 2;
	КонецЦикла;

	// Цикл по ТЗ.
	ТЗ = Новый ТаблицаЗначений;
	ТЗ.Колонки.Добавить("Имя", Новый ОписаниеТипов("Строка"));
	ТЗ.Колонки.Добавить("Сумма", Новый ОписаниеТипов("Число"));

	Для Каждого СтрокаТЗ Из ТЗ Цикл
		ТекИмя = СтрокаТЗ.Имя;
		ТекСумма = СтрокаТЗ.Сумма;
	КонецЦикла;

	// Цикл по соответствию.
	Карта = Новый Соответствие;
	Карта.Вставить("К1", 100);
	Карта.Вставить("К2", "value");

	Для Каждого КЗ Из Карта Цикл
		КлКЗ = КЗ.Ключ;
		ЗнКЗ = КЗ.Значение;
	КонецЦикла;

	// Цикл по структуре.
	Стр = Новый Структура("X, Y, Z", 1, 2, 3);
	Для Каждого ПараСтр Из Стр Цикл
		КлПары = ПараСтр.Ключ;
		ЗнПары = ПараСтр.Значение;
	КонецЦикла;

	// Цикл по СписокЗначений.
	СписокЗн = Новый СписокЗначений;
	СписокЗн.Добавить(1, "первый");
	СписокЗн.Добавить(2, "второй");

	Для Каждого ЭлСп Из СписокЗн Цикл
		ЗнСп = ЭлСп.Значение;
		ПредСп = ЭлСп.Представление;
	КонецЦикла;

	// Вложенные циклы.
	Для Каждого ВнешнийСтр Из Строки Цикл
		Для Каждого ВнутрСтр Из Числа Цикл
			Комб = ВнешнийСтр + "-" + Строка(ВнутрСтр);
		КонецЦикла;
	КонецЦикла;
КонецПроцедуры
