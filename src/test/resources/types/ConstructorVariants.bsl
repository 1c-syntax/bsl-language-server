Процедура Тест()
	// Массив constructors.
	М1 = Новый Массив;
	М2 = Новый Массив();
	М3 = Новый Массив(10);
	М4 = Новый Массив(3, 5);
	М5 = Новый Массив(М1);

	// Структура constructors.
	С1 = Новый Структура;
	С2 = Новый Структура();
	С3 = Новый Структура("Имя");
	С4 = Новый Структура("Имя", "значение");
	С5 = Новый Структура("Имя, Возраст", "Иван", 30);
	С6 = Новый Структура("К1, К2, К3", 1, "два", Истина);

	// Соответствие constructors.
	Со1 = Новый Соответствие;
	Со2 = Новый Соответствие();

	// ФиксированныеВарианты.
	ФС = Новый ФиксированнаяСтруктура(С5);
	ФСо = Новый ФиксированноеСоответствие(Со1);
	ФМ = Новый ФиксированныйМассив(М1);

	// Дата constructors.
	Д1 = Новый Дата(2020, 1, 1);
	Д2 = Новый Дата(2020, 6, 15, 12, 30, 45);

	// ТаблицаЗначений.
	ТЗ = Новый ТаблицаЗначений;
	КолТЗ = ТЗ.Колонки;

	// УникальныйИдентификатор.
	УИД1 = Новый УникальныйИдентификатор;
	УИД2 = Новый УникальныйИдентификатор("00000000-0000-0000-0000-000000000000");

	// ДвоичныеДанные.
	ДД1 = Новый ДвоичныеДанные;

	// COMОбъект.
	// (Skip — many constructors).

	// СписокЗначений.
	СЗ = Новый СписокЗначений;

	// ОписаниеТипов.
	ОТ1 = Новый ОписаниеТипов;
	ОТ2 = Новый ОписаниеТипов("Число");
	ОТ3 = Новый ОписаниеТипов("Число, Строка");

	// XML.
	ЗапXML = Новый ЗаписьXML;
	ЧтXML = Новый ЧтениеXML;

	// JSON.
	ЗапJSON = Новый ЗаписьJSON;
	ЧтJSON = Новый ЧтениеJSON;
КонецПроцедуры
