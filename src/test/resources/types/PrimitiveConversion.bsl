Процедура Тест()
	// Преобразования через Строка/Число/Булево/Дата.
	Стр1 = Строка(100);
	Стр2 = Строка(Истина);
	Стр3 = Строка('20200101');

	Чис1 = Число("123");
	Чис2 = Число("3.14");

	Бул1 = Булево(0);
	Бул2 = Булево("Истина");

	Дат1 = Дата(2020, 1, 1);
	Дат2 = Дата("20200101");

	// Тип() возвращает объект типа Тип.
	Т1 = Тип("Число");
	Т2 = Тип("Строка");

	// ТипЗнч() возвращает Тип.
	ТЗ = ТипЗнч(100);

	// Преобразование через xmlString/xmlValue.
	XmlЗнч = XMLСтрока(Истина);

	// Преобразование через формат.
	Форм = Формат(100, "ЧЦ=10; ЧДЦ=2");

	// Преобразование с СтрШаблон.
	Шаблон = СтрШаблон("Значение: %1, ещё: %2", "первое", 2);
КонецПроцедуры
