// Фикстура сверки с методической рекомендацией «Типизация кода», раздел «Лучшие практики
// написания типизированного кода».
// Имя пробы содержит номер пункта рекомендации: «Проба_4_20» это пункт 4.20.

#Область ОписаниеПеременных

Перем ЧисловаяПеременнаяМодуля; // Число -
Перем ОбъектДанныхМодуля; // см. ОбщегоНазначения.НовыеСвойстваПодписи
Перем ИнициализируемаяВТелеМодуля;

#КонецОбласти

#Область ИнициализацияЛокальныхПеременных

Процедура ПеремПротивИнициализацииЗначением(ТабличнаяЧасть)

	Перем ТекущаяСсылка;

	Для Каждого СтрокаТЧ Из ТабличнаяЧасть Цикл
		ТекущаяСсылка = СтрокаТЧ.Ссылка;
	КонецЦикла;

	Проба_4_1_1 = ТекущаяСсылка;
	Использование(Проба_4_1_1);

	ДругаяСсылка = Справочники.Справочник1.ПустаяСсылка();
	Для Каждого СтрокаТЧ Из ТабличнаяЧасть Цикл
		ДругаяСсылка = СтрокаТЧ.Ссылка;
	КонецЦикла;

	Проба_4_1_2 = ДругаяСсылка;
	Использование(Проба_4_1_2);

КонецПроцедуры

Процедура ИнициализацияДоУсловия(ТелоСообщения)

	Идентификатор = "";

	Если ТелоСообщения.Свойство("Идентификатор") Тогда
		Идентификатор = ТелоСообщения.Идентификатор;
	КонецЕсли;

	Проба_4_2 = Идентификатор;
	Использование(Проба_4_2);

КонецПроцедуры

Процедура УточнениеТипаСтрочнымКомментарием()

	Проба_4_4 = СоздатьНовыйОбъектПоТипу(); // СправочникОбъект.Справочник1 -
	Использование(Проба_4_4);

	Проба_4_5 = СоздатьНовыйОбъектПоТипу(); // см. НовыйОбъектДанных
	Использование(Проба_4_5);

КонецПроцедуры

Функция СоздатьНовыйОбъектПоТипу()

	Возврат Неопределено;

КонецФункции

Процедура ПрименениеОписанияТипов()

	Типизатор = Новый ОписаниеТипов("СправочникСсылка.Справочник1");
	Проба_4_6 = Типизатор.ПривестиЗначение(Неопределено);
	Использование(Проба_4_6);

КонецПроцедуры

Процедура ПеременныеМодуля()

	Проба_4_7 = ЧисловаяПеременнаяМодуля;
	Использование(Проба_4_7);

	Проба_4_8 = ОбъектДанныхМодуля;
	Использование(Проба_4_8);

	Проба_4_9 = ИнициализируемаяВТелеМодуля;
	Использование(Проба_4_9);

КонецПроцедуры

#КонецОбласти

#Область ИнициализацияКлючейСтруктуры

Процедура КлючиСтруктуры()

	Параметры = Новый Структура("Ссылка", Справочники.Справочник1.ПустаяСсылка());
	Проба_4_10 = Параметры.Ссылка;
	Использование(Проба_4_10);

	ДругиеПараметры = Новый Структура;
	ДругиеПараметры.Вставить("Ссылка", Справочники.Справочник1.ПустаяСсылка());
	Проба_4_11 = ДругиеПараметры.Ссылка;
	Использование(Проба_4_11);

КонецПроцедуры

Процедура КлючВставленВДругойОбластиВидимости(НеобходимоЗаполнить)

	Параметры = Новый Структура;
	Если НеобходимоЗаполнить Тогда
		ЗаполнитьПараметры(Параметры);
	КонецЕсли;

	Проба_4_13 = Параметры.МоеСвойство;
	Использование(Проба_4_13);

КонецПроцедуры

Процедура ЗаполнитьПараметры(Параметры)

	Параметры.Вставить("МоеСвойство", "Новое значение");

КонецПроцедуры

Процедура БезопасныйДоступКПолюСтруктуры(Структура)

	Если Структура.Свойство("МоеСвойство") И ЗначениеЗаполнено(Структура.МоеСвойство) Тогда
		Проба_4_14 = Структура.МоеСвойство; // СправочникСсылка.Справочник1 -
		Использование(Проба_4_14);
	КонецЕсли;

КонецПроцедуры

Процедура ПриемникМетодаСвойство(Структура)

	МояПеременная = "";

	Если Структура.Свойство("МоеСвойство", МояПеременная) Тогда
		Проба_4_15 = МояПеременная;
		Использование(Проба_4_15);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ОписаниеМассивов

Процедура ТипЭлементовМассиваВСтроке()

	СписокСсылок = Новый Массив; // Массив из СправочникСсылка.Справочник1 -
	Для Каждого Ссылка Из СписокСсылок Цикл
		Проба_4_16 = Ссылка;
		Использование(Проба_4_16);
	КонецЦикла;

КонецПроцедуры

// Возвращаемое значение:
//  Массив из СправочникСсылка.Справочник1
Функция НовыйСписокНоменклатуры()

	Возврат Новый Массив;

КонецФункции

Процедура ТипЭлементовМассиваИзКонструктора()

	СписокСсылок = НовыйСписокНоменклатуры();
	Для Каждого Ссылка Из СписокСсылок Цикл
		Проба_4_17 = Ссылка;
		Использование(Проба_4_17);
	КонецЦикла;

КонецПроцедуры

// Параметры:
//  Список - Массив из СправочникСсылка.Справочник1 -
Процедура ТипЭлементовМассиваВОписанииПараметра(Список)

	Для Каждого Ссылка Из Список Цикл
		Проба_4_19 = Ссылка;
		Использование(Проба_4_19);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область ОписаниеТаблицыЗначений

// Возвращаемое значение:
//  ТаблицаЗначений:
//  * Номенклатура - СправочникСсылка.Справочник1
//  * Количество - Число
Функция ТоварыДляСписания()

	Возврат Новый ТаблицаЗначений;

КонецФункции

Процедура КолонкиТаблицыИзВозвращаемогоЗначения()

	Таблица = ТоварыДляСписания();
	Для Каждого СтрокаТаблицы Из Таблица Цикл
		Проба_4_20 = СтрокаТаблицы.Номенклатура;
		Использование(Проба_4_20);
	КонецЦикла;

КонецПроцедуры

// Параметры:
//  Таблица - См. ТоварыДляСписания
Процедура ТаблицаПараметромПоСсылкеНаКонструктор(Таблица)

	Для Каждого СтрокаТаблицы Из Таблица Цикл
		Проба_4_21 = СтрокаТаблицы.Номенклатура;
		Использование(Проба_4_21);
	КонецЦикла;

КонецПроцедуры

// Параметры:
//  Таблица - ТаблицаЗначений - произвольная таблица с товаром:
//  * Номенклатура - СправочникСсылка.Справочник1
Процедура ПроизвольнаяТаблицаСОписаниемКолонок(Таблица)

	Для Каждого СтрокаТаблицы Из Таблица Цикл
		Проба_4_22 = СтрокаТаблицы.Номенклатура;
		Использование(Проба_4_22);
	КонецЦикла;

КонецПроцедуры

// Параметры:
//  Таблица - См. ТоварыДляСписания
//  ИмяКолонки - Строка -
Процедура ОбращениеККолонкеПоИмениВПеременной(Таблица, ИмяКолонки)

	Для Каждого СтрокаТаблицы Из Таблица Цикл
		Проба_4_23 = СтрокаТаблицы[ИмяКолонки];
		Использование(Проба_4_23);
	КонецЦикла;

КонецПроцедуры

// Параметры:
//  СтрокаТаблицы - СтрокаТаблицыЗначений:
//  * Номенклатура - СправочникСсылка.Справочник1
Процедура СтрокаТаблицыСПеречислениемКолонок(СтрокаТаблицы)

	Проба_4_24 = СтрокаТаблицы.Номенклатура;
	Использование(Проба_4_24);

КонецПроцедуры

// Параметры:
//  СтрокаТаблицы - СтрокаТаблицыЗначений: См. ТоварыДляСписания
Процедура СтрокаТаблицыПоСсылкеНаКонструктор(СтрокаТаблицы)

	Проба_4_25 = СтрокаТаблицы.Номенклатура;
	Использование(Проба_4_25);

КонецПроцедуры

#КонецОбласти

#Область ОписаниеСоответствияИСпискаЗначений

// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - Строка
//  * Значение - СправочникСсылка.Справочник1
Функция НовоеСоответствие()

	Возврат Новый Соответствие;

КонецФункции

Процедура ОписаниеСоответствия()

	Данные = НовоеСоответствие();
	Для Каждого КлючЗначение Из Данные Цикл
		Проба_4_26_1 = КлючЗначение.Ключ;
		Использование(Проба_4_26_1);
		Проба_4_26_2 = КлючЗначение.Значение;
		Использование(Проба_4_26_2);
	КонецЦикла;

	МоеСоответствие = Новый Соответствие; // Соответствие из Строка
	Проба_4_27 = МоеСоответствие;
	Использование(Проба_4_27);

	Для Каждого ЭлементСоответствия Из МоеСоответствие Цикл
		Проба_4_27_Значение = ЭлементСоответствия.Значение;
		Использование(Проба_4_27_Значение);
	КонецЦикла;

	ПоместитьВоВременноеХранилище(Данные, "адрес");
	Проба_4_28 = ПолучитьИзВременногоХранилища("адрес"); // См. НовоеСоответствие
	Использование(Проба_4_28);

КонецПроцедуры

// Возвращаемое значение:
//  СписокЗначений из СправочникСсылка.Справочник1
Функция НовыйСписокДокументов()

	Возврат Новый СписокЗначений;

КонецФункции

// Возвращаемое значение:
//  СписокЗначений из СправочникСсылка.Справочник1
Функция ПолучитьСписокДокументов()

	СписокДокументов = Новый СписокЗначений;
	СписокДокументов.Добавить(Справочники.Справочник1.ПустаяСсылка());
	Возврат СписокДокументов;

КонецФункции

Процедура ОписаниеСпискаЗначений()

	СписокДокументов = НовыйСписокДокументов();
	Проба_4_29 = СписокДокументов.Добавить();
	Использование(Проба_4_29);

	НовыйЭлемент = СписокДокументов.Добавить();
	Проба_4_30 = НовыйЭлемент.Значение;
	Использование(Проба_4_30);

	МойСписок = Новый СписокЗначений; // СписокЗначений из Строка
	Проба_4_31 = МойСписок;
	Использование(Проба_4_31);

	Для Каждого ЭлементМоегоСписка Из МойСписок Цикл
		Проба_4_31_Значение = ЭлементМоегоСписка.Значение;
		Использование(Проба_4_31_Значение);
	КонецЦикла;

	ЗаполненныйСписок = ПолучитьСписокДокументов();
	Для Каждого ЭлементСписка Из ЗаполненныйСписок Цикл
		Проба_4_32 = ЭлементСписка.Значение;
		Использование(Проба_4_32);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область ФункцииКонструкторы

// Возвращаемое значение:
//  Структура:
//  * Ссылка - СправочникСсылка.Справочник1
//  * Количество - Число
Функция НовыйОбъектДанных()

	Возврат Новый Структура;

КонецФункции

Процедура КонструкторСложногоОбъектаДанных()

	Проба_4_33 = НовыйОбъектДанных();
	Использование(Проба_4_33);

КонецПроцедуры

// Параметры:
//  КомандыПечати - ТаблицаЗначений - состав полей см. в функции ТоварыДляСписания.
Процедура СоставПолейСмВФункции(КомандыПечати)

	Проба_4_34 = КомандыПечати;
	Использование(Проба_4_34);

КонецПроцедуры

// Заполняет список команд печати.
//
//  состав полей см. в функции ТоварыДляСписания.
//
Процедура СсылкаВОписанииМетода(КомандыПечати)

	Проба_4_35 = КомандыПечати;
	Использование(Проба_4_35);

КонецПроцедуры

//  см. ТоварыДляСписания.
//
Процедура ГолаяСсылкаСтрокойОписания(КомандыПечати)

	Проба_4_36 = КомандыПечати;
	Использование(Проба_4_36);

КонецПроцедуры

// Параметры:
//  КомандыПечати - см. ТоварыДляСписания.
Процедура СсылкаСТочкойВКонце(КомандыПечати)

	Проба_4_37 = КомандыПечати;
	Использование(Проба_4_37);

КонецПроцедуры

// Параметры:
//  КомандыПечати - см. ТоварыДляСписания - команды для заполнения
Процедура СсылкаСОписаниемПосле(КомандыПечати)

	Проба_4_38 = КомандыПечати;
	Использование(Проба_4_38);

КонецПроцедуры

// Параметры:
//  КомандыПечати - см. ТоварыДляСписания
Процедура СсылкаБезБазовогоТипаИОписания(КомандыПечати)

	Проба_4_39 = КомандыПечати;
	Использование(Проба_4_39);

КонецПроцедуры

// Возвращаемое значение:
//  ДеревоЗначений:
//  * Наименование - Строка
//  * ВидЭлемента - Число
Функция ДеревоНовыхЭлементов()

	Возврат Новый ДеревоЗначений;

КонецФункции

Процедура ФункцияПолучательСложногоОбъекта()

	ДеревоЭлементов = ДеревоНовыхЭлементов();
	Для Каждого СтрокаДерева Из ДеревоЭлементов.Строки Цикл
		Проба_4_40 = СтрокаДерева.Наименование;
		Использование(Проба_4_40);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область ОписаниеПрограммныхИнтерфейсов

// Параметры:
//  Объект - СправочникОбъект.Справочник1 - где:
//   * ТабличнаяЧасть1 - ТабличнаяЧасть Из СтрокаТабличнойЧасти:
//    ** Свойство - СправочникСсылка.Справочник1 -
Процедура ИнтерфейсОбъектаЧерезТипЭлемента(Объект)

	Для Каждого СтрокаТЧ Из Объект.ТабличнаяЧасть1 Цикл
		Проба_4_41 = СтрокаТЧ.Свойство;
		Использование(Проба_4_41);
	КонецЦикла;

КонецПроцедуры

// Параметры:
//  Объект - СправочникОбъект.Справочник1 - где:
//   * ТабличнаяЧасть1 - ТабличнаяЧасть, ДанныеФормыКоллекция:
//    ** Свойство - СправочникСсылка.Справочник1 -
Процедура РасширениеПолейНаСамойКоллекции(Объект)

	Для Каждого СтрокаТЧ Из Объект.ТабличнаяЧасть1 Цикл
		Проба_4_42 = СтрокаТЧ.Свойство;
		Использование(Проба_4_42);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область РаботаСФормами

// Параметры:
//  Форма - ФормаКлиентскогоПриложения:
//  * Объект - СправочникОбъект.Справочник1 - основной реквизит формы
//  * Элементы - ВсеЭлементыФормы:
//   ** Товары - ТаблицаФормы - элемент таблицы товаров
Процедура ОписаниеЧастиФормы(Форма)

	Проба_4_43 = Форма.Объект;
	Использование(Проба_4_43);

	Проба_4_44 = Форма.Элементы.Товары;
	Использование(Проба_4_44);

КонецПроцедуры

// Параметры:
//  Форма - см. Справочник.Справочник1.Форма.ФормаЭлемента
Процедура СсылкаНаКонкретнуюФорму(Форма)

	Проба_4_45 = Форма.Объект;
	Использование(Проба_4_45);

	Проба_4_45_Ссылка = Форма.Объект.Ссылка;
	Использование(Проба_4_45_Ссылка);

	Проба_4_45_Элемент = Форма.Элементы.Наименование;
	Использование(Проба_4_45_Элемент);

КонецПроцедуры

// Параметры:
//  Форма - РасширениеУправляемойФормыДляОбъектов -
Процедура РасширениеУправляемойФормы(Форма)

	Проба_4_46 = Форма;
	Использование(Проба_4_46);

КонецПроцедуры

Процедура ПолучениеИОткрытиеФормы()

	Проба_4_47 = ПолучитьФорму("Справочник.Справочник1.Форма.ФормаЭлемента");
	Использование(Проба_4_47);
	Проба_4_47_Ссылка = Проба_4_47.Объект.Ссылка;
	Использование(Проба_4_47_Ссылка);

	Проба_4_48 = Справочники.Справочник1.ПолучитьФорму("ФормаЭлемента");
	Использование(Проба_4_48);
	Проба_4_48_Ссылка = Проба_4_48.Объект.Ссылка;
	Использование(Проба_4_48_Ссылка);

	ИмяФормы = "Справочник.Справочник1.Форма.ФормаЭлемента";
	Проба_4_49 = ПолучитьФорму(ИмяФормы);
	Использование(Проба_4_49);

	Проба_4_50 = ПолучитьФорму("Справочник.Справочник1.ФормаОбъекта");
	Использование(Проба_4_50);

КонецПроцедуры

// Параметры:
//  Параметры - Структура - параметры формы
Процедура ЧтениеПоСтроковомуИндексуСоСтрочнымТипом(Параметры)

	Если Параметры.Свойство("Ссылка") Тогда
		Проба_4_58 = Параметры["Ссылка"]; // СправочникСсылка.Справочник1 -
		Использование(Проба_4_58);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область КонтейнерыВыборкиМакеты

Процедура ВременноеХранилищеИКонтейнеры(ДополнительныеПараметры)

	Проба_4_53 = ПолучитьИзВременногоХранилища("адрес"); // см. НовыйОбъектДанных
	Использование(Проба_4_53);

	Проба_4_54 = ДополнительныеПараметры.МоиДанные; // см. НовыйОбъектДанных
	Использование(Проба_4_54);

КонецПроцедуры

// Возвращаемое значение:
//  ВыборкаИзРезультатаЗапроса:
//  * Номенклатура - СправочникСсылка.Справочник1
Функция ОстаткиДляОбработки()

	Запрос = Новый Запрос;
	Возврат Запрос.Выполнить().Выбрать();

КонецФункции

Процедура ВыборкаИзРезультатаЗапроса()

	ВыборкаОстатков = ОстаткиДляОбработки();
	Пока ВыборкаОстатков.Следующий() Цикл
		Проба_4_63 = ВыборкаОстатков.Номенклатура;
		Использование(Проба_4_63);
	КонецЦикла;

	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ Ссылка ИЗ Справочник.Справочник1";
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		Проба_4_64 = Выборка.Ссылка;
		Использование(Проба_4_64);
	КонецЦикла;

КонецПроцедуры

Процедура ПередачаОбъектаВДругойМодуль()

	Проба_4_65 = ОбщегоНазначения.НовыеСвойстваПодписи();
	Использование(Проба_4_65);

КонецПроцедуры

Процедура ТипМакетаВСтроке()

	Проба_4_66 = ПолучитьОбщийМакет("ИмяМакета"); // см. Справочник.Справочник1.Макет.Макет
	Использование(Проба_4_66);

КонецПроцедуры

// Возвращаемое значение:
//  Структура, СправочникОбъект.Справочник1 - смешанный тип объекта-копии:
//  * ДополнительнаяКолонка - Строка -
Функция НовыйОбъектКопия()

	Возврат Новый Структура;

КонецФункции

Процедура ОбъектКопияСоСмешаннымТипом()

	Проба_4_67 = НовыйОбъектКопия();
	Использование(Проба_4_67);

	Проба_4_67_Колонка = НовыйОбъектКопия().ДополнительнаяКолонка;
	Использование(Проба_4_67_Колонка);

	Проба_4_67_Реквизит = НовыйОбъектКопия().Реквизит1;
	Использование(Проба_4_67_Реквизит);

КонецПроцедуры

#КонецОбласти

Процедура Использование(Значение)

	Возврат;

КонецПроцедуры

ЧисловаяПеременнаяМодуля = 0;
ОбъектДанныхМодуля = ОбщегоНазначения.НовыеСвойстваПодписи();
ИнициализируемаяВТелеМодуля = Справочники.Справочник1.ПустаяСсылка();
