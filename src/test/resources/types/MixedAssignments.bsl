Процедура Тест()
	// Один и тот же variable но с разными типами.
	У1 = 100;
	У2 = "string";
	У3 = Истина;

	// Условное переопределение.
	А = "string";
	Если 1 > 0 Тогда
		А = 100;
	КонецЕсли;

	// Через цикл.
	Б = "init";
	Для Сч = 1 По 3 Цикл
		Б = Б + " итерация";
	КонецЦикла;

	// Через try-catch.
	В = "init";
	Попытка
		В = 100 / 0;
	Исключение
		В = "ошибка";
	КонецПопытки;

	// Чтение local from outer scope.
	НарВнеш = "наружу";
	Если Истина Тогда
		ВнутрВал = НарВнеш;
	КонецЕсли;

	// Multiple targets.
	Г = "first";
	Г = "second";
	Г = "third";

	// Цепочка assignment в одну строку (BSL не поддерживает многоцелевых).
	// Просто sequential.
	Д = 100;
	Д2 = Д;
	Д3 = Д2;
КонецПроцедуры
