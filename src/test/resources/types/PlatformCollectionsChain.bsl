Процедура Использование()
	// Chain через .Скопировать().
	М1 = Новый Массив;
	М1.Добавить(1);
	М1.Добавить(2);
	М2 = М1.Скопировать();
	КолМ2 = М2.Количество();

	// Chain через индексный доступ.
	Эл = М1[0];
	КопияЭл = М1[М1.ВГраница()];

	// Соответствие с разными типами значений.
	К = Новый Соответствие;
	К.Вставить("Число", 42);
	К.Вставить("Строка", "hello");
	К.Вставить("Булево", Истина);
	К.Вставить("Дата", '20200101');

	ЗнЧисло = К["Число"];
	ЗнСтрока = К["Строка"];
	ЗнБул = К["Булево"];
	ЗнДата = К["Дата"];

	// Цикл по Соответствию и по структуре.
	Стр = Новый Структура("X, Y, Z", 1, "two", Истина);
	Для Каждого Пара Из Стр Цикл
		КлючПары = Пара.Ключ;
		ЗначПары = Пара.Значение;
	КонецЦикла;

	// СписокЗначений.
	СписокЗн = Новый СписокЗначений;
	СписокЗн.Добавить(1);
	СписокЗн.Добавить("два");
	КолСписка = СписокЗн.Количество();
	ЭлСписка = СписокЗн[0];

	// ДеревоЗначений.
	Дерево = Новый ДеревоЗначений;
	Дерево.Колонки.Добавить("Имя");

	// XDTOFactory.
	Фабрика = XDTOФабрика;

	// БуфферДанных.
	БД = Новый ДвоичныеДанные("/tmp/fake");
КонецПроцедуры
