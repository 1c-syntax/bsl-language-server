Процедура Тест()
	// Базовые boolean выражения.
	А = Истина;
	Б = Ложь;

	// Логические операции.
	АИБ = А И Б;
	АИЛИБ = А ИЛИ Б;
	НА = НЕ А;

	// Цепочки.
	Цеп1 = А И Б ИЛИ НЕ А;
	Цеп2 = (А ИЛИ Б) И (НЕ А ИЛИ Б);

	// Сравнения возвращающие булево.
	Срав1 = 1 = 1;
	Срав2 = 1 <> 2;
	Срав3 = 1 > 0;
	Срав4 = "a" = "a";
	Срав5 = '20200101' = '20200101';

	// Булево в условии Если.
	Если А И Б Тогда
		Рез1 = "оба";
	ИначеЕсли А ИЛИ Б Тогда
		Рез1 = "хоть один";
	Иначе
		Рез1 = "ни одного";
	КонецЕсли;

	// Булево в Пока.
	Сч = 0;
	Пока Сч < 10 И А Цикл
		Сч = Сч + 1;
	КонецЦикла;

	// Булево как параметр.
	Сообщить(А И Б);

	// Булево в Возврат (внутри проц не имеет смысла, но смысл — выражение).
	БулВыр = (НЕ Б) И (А ИЛИ (1 < 2));
КонецПроцедуры
