Перем Счетчик;

Функция Корень()
    Результат = Узел();
    Возврат Результат;
КонецФункции

Функция Узел()
    Счетчик = 1;
    Вложенный = Ветка();
    Результат = Новый Структура("Вложенный", Вложенный);
    Возврат Результат;
КонецФункции

Функция Ветка()
    Возврат Узел();
КонецФункции
