Процедура Тест()
	// 1. Индексный доступ на ТЗ → СтрокаТаблицыЗначений.
	ТЗ = Новый ТаблицаЗначений;
	ТЗ.Колонки.Добавить("Имя");
	ПерваяСтрока = ТЗ[0];
	X = ПерваяСтрока.Имя;

	// 2. Цепочка с глобальным методом — СтрНайти возвращает Число.
	Поз = СтрНайти("hello", "l");

	// 3. Сложение через переменные — тип определяется левым.
	Прив = "X" + ТЗ.Количество();

	// 4. Использование Произвольный (Any-тип).
	ВыводимыйТип = Тип("Число");
КонецПроцедуры

// 5. Функция, возвращающая Массив (через JsDoc) — для inferMethodCall на
//    локальную функцию с типизированным возвратом.
//
// Возвращаемое значение:
//  Массив - список значений
//
Функция ПолучитьМассив() Экспорт
	Возврат Новый Массив;
КонецФункции

Процедура ИспользоватьМассив()
	Список = ПолучитьМассив();
КонецПроцедуры

// 6. Функция без JsDoc — return-type unknown.
Функция Аноним() Экспорт
	Возврат 1;
КонецФункции
