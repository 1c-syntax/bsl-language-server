Процедура Тест()
	// Динамический конструктор с строковым именем (Новый "ИмяТипа").
	ИмяТипа = "Массив";
	Объект1 = Новый (ИмяТипа);

	// Конструктор с прямой строковой константой (через "стрипping" кавычек).
	Объект2 = Новый ("Структура");
КонецПроцедуры
