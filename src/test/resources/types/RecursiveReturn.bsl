Функция УзелДерева(Исходный) Экспорт

	Узел = Новый Структура;
	Узел.Вставить("Имя", "");
	Узел.Вставить("Вложенный", УзелДерева(Исходный));

	Возврат Узел;

КонецФункции

Процедура Проверка() Экспорт

	Дерево = УзелДерева(Неопределено);

КонецПроцедуры
