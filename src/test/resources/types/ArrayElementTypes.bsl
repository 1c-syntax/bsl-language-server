// Возвращаемое значение:
//  Массив из Число
Функция Числа()
	Возврат Новый Массив;
КонецФункции

// Возвращаемое значение:
//  Массив из Строка
Функция Строки()
	Возврат Новый Массив;
КонецФункции

// Возвращаемое значение:
//  Массив из Число, Строка
Функция ЧислаИлиСтроки()
	Возврат Новый Массив;
КонецФункции

Процедура Тест()
	СписокЧисел = Числа();
	СписокСтрок = Строки();
	СмешанныйСписок = ЧислаИлиСтроки();
КонецПроцедуры
