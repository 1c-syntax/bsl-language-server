// Возвращаемое значение:
//  Массив из Число
Функция Числа()
	Возврат Новый Массив;
КонецФункции

// Возвращаемое значение:
//  Массив из Строка
Функция Строки()
	Возврат Новый Массив;
КонецФункции

Процедура Тест()
	СписокЧисел = Числа();
	СписокСтрок = Строки();
КонецПроцедуры
