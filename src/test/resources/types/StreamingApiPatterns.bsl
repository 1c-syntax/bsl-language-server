Процедура Тест()
	// Стек поток-API (ЗаписьJSON / ЧтениеJSON / ЗаписьXML / ЧтениеXML).
	ЗапJSON = Новый ЗаписьJSON;
	ЗапJSON.УстановитьСтроку();
	Результат = ЗапJSON.Закрыть();

	ЧтJSON = Новый ЧтениеJSON;
	ЧтJSON.УстановитьСтроку("{}");

	ЗапXML = Новый ЗаписьXML;
	ЗапXML.УстановитьСтроку();
	XmlРез = ЗапXML.Закрыть();

	ЧтXML = Новый ЧтениеXML;
	ЧтXML.УстановитьСтроку("<root/>");

	// Запрос (Database query).
	Запрос = Новый Запрос;
	Запрос.Текст = "SELECT * FROM Документ.Чек";

	// HTTPСоединение и HTTPЗапрос.
	Соед = Новый HTTPСоединение("example.com");
	Зап = Новый HTTPЗапрос("/api/users");

	// ТекстовыйДокумент.
	ТекстДок = Новый ТекстовыйДокумент;
	ТекстДок.УстановитьТекст("Привет");

	// Файл / Поток.
	Файл = Новый Файл("/tmp/test.txt");
	ПотокФ = Новый ФайловыйПоток("/tmp/test.txt", РежимОткрытияФайла.Открыть);
КонецПроцедуры
