Процедура Пример3()
	ТелоЗапроса = Тело();
	Для Каждого Элемент Из ТелоЗапроса Цикл
		КлючЭлемента = Элемент.Ключ;
		ЗначениеЭлемента = Элемент.Значение;
	КонецЦикла;
КонецПроцедуры

// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//   * Ключ - Строка
//   * Значение - Число
Функция Тело() Экспорт
	Результат = Новый Соответствие;
	Результат.Вставить("count", 10);
	Возврат Результат;
КонецФункции
