Процедура Тест()
	// Унарные минусы.
	А = -10;
	Б = -(-20);
	В = -А;
	Г = -(А + Б);

	// Унарные плюсы.
	Д = +10;
	Е = +А;
	Ж = +(А + Б);

	// НЕ операторы.
	З = НЕ Истина;
	И1 = НЕ З;
	К = НЕ (10 > 5);
	Л = НЕ НЕ Истина;
	М = НЕ (Истина И Ложь);

	// Смесь унарных и бинарных.
	Н = -10 + 20;
	О = -(10 + 20);
	П = НЕ Истина ИЛИ Ложь;
	Р = НЕ (10 > 5) И (5 < 10);

	// Унарные в условии.
	Если НЕ Истина Тогда
		Сток1 = "не";
	Иначе
		Сток1 = "да";
	КонецЕсли;

	// Унарные в тернарном.
	Т = ?(НЕ Ложь, "первая", "вторая");
КонецПроцедуры
