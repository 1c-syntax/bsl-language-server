Процедура Использование()
	// Глобальные функции с типизированными параметрами.
	НашлиИндекс = СтрНайти("главная строка", "стр");
	Замена = СтрЗаменить("привет мир", "привет", "пока");
	Поделили = СтрРазделить("a,b,c,d", ",");

	// Конструкторы с разной арностью.
	ПустМ = Новый Массив;
	НеПустМ = Новый Массив(10);
	МногомерМ = Новый Массив(3, 2);
	КопияМ = Новый Массив(ПустМ);

	СтрВ = Новый Структура;
	СтрНаз = Новый Структура("Имя", "значение");
	СтрМногоИм = Новый Структура("К1, К2, К3", 1, 2, 3);

	СооВ = Новый Соответствие;

	ТЗ = Новый ТаблицаЗначений;

	// Вызов метода с разными типами параметров.
	НеПустМ.Добавить(99);
	НеПустМ.Вставить(0, 999);
	НеПустМ.Удалить(0);

	// Глобальная функция в составе выражения.
	Сумма = СтрДлина("слово") + СтрДлина("ещё одно");

	// Вызов с переменной как аргумент (не литерал).
	Стр = "тест";
	Длина = СтрДлина(Стр);
КонецПроцедуры
