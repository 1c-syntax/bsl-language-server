// inferCall на неизвестное → EMPTY.
НеизвМетод = НетТакогоМетода1234567(1);

// Конструктор неизвестного типа — EMPTY.
НеизвТип = Новый НетТакогоТипа99999();

// Dereference на EMPTY — EMPTY.
Несуществующее = НетПеременной987.Поле1.Поле2;
