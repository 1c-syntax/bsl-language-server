Процедура Тест()
	// Структура с известными полями.
	Стр = Новый Структура("Имя, Возраст", "Иван", 30);
	А = Стр.Имя;

	// Массив с методами.
	М = Новый Массив;
	М.Добавить(1);

	// ТЗ с колонками.
	ТЗ = Новый ТаблицаЗначений;
	ТЗ.Колонки.Добавить("К1");
	ТЗ.Колонки.Добавить("К2");

	// Соответствие.
	Со = Новый Соответствие;
	Со.Вставить("X", 1);

	// Дата.
	Д = Новый Дата(2020, 1, 1);

	// УникальныйИдентификатор.
	УИД = Новый УникальныйИдентификатор;
КонецПроцедуры
