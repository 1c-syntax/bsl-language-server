// Структура с пустым элементом среди ключей (между запятыми).
СтрПустойКлюч = Новый Структура(",Имя, ,Возраст", "Иван", 30);

// Структура с большим числом ключей, чем values — оставшиеся ключи получают Неопределено.
СтрБольшеКлючей = Новый Структура("К1, К2, К3", 100);

// Структура без ключей — keyLiteral пуст, ключи не добавляются.
ПустаяСтр = Новый Структура();

// Структура с не-строковым первым аргументом — keyLiteral null.
НеСтрКлюч = Новый Структура(КакойТоМассив);
