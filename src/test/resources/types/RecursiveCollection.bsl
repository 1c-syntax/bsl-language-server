Процедура Пример()
	Ветви = Ветки();
	Вложенная = Ветки()[0];
	Глубоко = Ветки()[0][0];
КонецПроцедуры

// Возвращаемое значение:
//  Массив из см. Ветки - Ветки дерева.
Функция Ветки() Экспорт
	Возврат Новый Массив;
КонецФункции
