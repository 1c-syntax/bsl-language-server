Процедура Тест()
	// Структура с .Вставить в разных сценариях.
	Стр1 = Новый Структура;
	Стр1.Вставить("ПростойКлюч", 100);
	Стр1.Вставить("Строка", "значение");
	Стр1.Вставить("Дата", '20200101');
	Стр1.Вставить("Булево", Истина);

	// Доступ через точку.
	А = Стр1.ПростойКлюч;
	Б = Стр1.Строка;
	В = Стр1.Дата;
	Г = Стр1.Булево;

	// Доступ через [].
	А2 = Стр1["ПростойКлюч"];
	Б2 = Стр1["Строка"];

	// Соответствие с разными типами.
	Карта = Новый Соответствие;
	Карта.Вставить("К1", 1);
	Карта.Вставить("К2", "two");
	Карта.Вставить("К3", '20200101');

	// Доступ к Соответствию через [].
	Д = Карта["К1"];
	Е = Карта["К2"];

	// Несуществующий ключ.
	Ж = Карта["несуществует"];

	// Структура с двумя .Вставить одного ключа (последний выигрывает).
	ДубКлюч = Новый Структура;
	ДубКлюч.Вставить("Поле", 1);
	ДубКлюч.Вставить("Поле", "обновлено");
	З = ДубКлюч.Поле;

	// Вложенная Структура.
	Внешн = Новый Структура;
	ВнутрСтр = Новый Структура;
	ВнутрСтр.Вставить("Глуб", 42);
	Внешн.Вставить("Внутр", ВнутрСтр);

	ВнутрФромВнешн = Внешн.Внутр;
	ГлубФромВнешн = ВнутрФромВнешн.Глуб;

	// ТЗ с разными типами колонок.
	ТЗ = Новый ТаблицаЗначений;
	ТЗ.Колонки.Добавить("СтрКол", Новый ОписаниеТипов("Строка"));
	ТЗ.Колонки.Добавить("ЧисКол", Новый ОписаниеТипов("Число"));
	ТЗ.Колонки.Добавить("ДатКол", Новый ОписаниеТипов("Дата"));
	ТЗ.Колонки.Добавить("БулКол", Новый ОписаниеТипов("Булево"));
	ТЗ.Колонки.Добавить("БезТипа");

	Для Каждого СтрокаТЗ Из ТЗ Цикл
		ИмК = СтрокаТЗ.СтрКол;
		К = СтрокаТЗ.ЧисКол;
		Л = СтрокаТЗ.ДатКол;
		М = СтрокаТЗ.БулКол;
		Н = СтрокаТЗ.БезТипа;
	КонецЦикла;
КонецПроцедуры
