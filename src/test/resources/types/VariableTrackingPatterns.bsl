Перем МодульПерем Экспорт;

Перем ВнутрПерем;

Процедура Тест()
	// Параметр процедуры.
	Парам = 42;

	// Локальная переменная.
	Локал = "строка";

	// Объявление через Перем внутри процедуры.
	Перем ОтлЛокал;
	ОтлЛокал = 100;

	// Один и тот же имя в разных областях видимости.
	Стр = "наружу";

	// Переопределение типа через несколько присваиваний.
	У = 1;
	У = "стало строкой";

	// Условное присваивание (если/иначе).
	Если Истина Тогда
		Условн = "ветка1";
	Иначе
		Условн = 100;
	КонецЕсли;

	// Цикл с инициализацией.
	Для Каждого Эл Из Новый Массив Цикл
		Эл2 = Эл;
	КонецЦикла;

	// Try-Catch с переменной в обоих ветках.
	Попытка
		Х = 10;
	Исключение
		Х = "ошибка";
	КонецПопытки;

	// Переменная-функция.
	ВызовФункции = СтрДлина("123");

	// Использование переменной как параметра.
	НеЯвный = Парам + 10;
КонецПроцедуры
