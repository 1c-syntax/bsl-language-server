// Возвращаемое значение:
//  Структура
//
Функция СоздатьСтруктуру() Экспорт
	С = Новый Структура;
	С.Вставить("X", 1);
	С.Вставить("Y", 2);
	Возврат С;
КонецФункции

// Локальная процедура — должна быть видна в noDot completion.
Процедура МояЛокальная()
КонецПроцедуры

// Экспортируемая функция.
Функция МояЭкспортная() Экспорт
КонецФункции

// Локальная переменная модуля.
Перем ГлобПерем Экспорт;

Процедура Использование()
	// Локальная переменная процедуры.
	ЛокПерем = 42;

	// Структура для dot-completion.
	Стр = СоздатьСтруктуру();

	// Массив для dot-completion.
	М = Новый Массив;
	М.Добавить(1);

	// ТЗ для dot-completion с колонками.
	ТЗ = Новый ТаблицаЗначений;
	ТЗ.Колонки.Добавить("К1");
	ТЗ.Колонки.Добавить("К2");

	// Соответствие для dot-completion.
	Со = Новый Соответствие;
КонецПроцедуры
