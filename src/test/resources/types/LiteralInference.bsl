// Покрытие inferLiteral для всех типов литералов.
СтроковыйЛитерал = "строка";
ЧисловойЛитерал = 12345;
ЛогическийИстина = Истина;
ЛогическийЛожь = Ложь;
ДатаЛитерал = '20200101';
ЛитералНеопределено = Неопределено;
ЛитералNull = NULL;

// Динамический индексатор Соответствия (не строковый литерал) — union по
// всем известным value-типам.
Карта = Новый Соответствие;
Карта.Вставить("К1", 100);
Карта.Вставить("К2", "строка");
ДинамКлюч = "К1";
ЗначениеПоДинамКлючу = Карта[ДинамКлюч];

// Неизвестный ключ Структуры с строковым литералом — пустой результат.
Стр = Новый Структура("Имя, Возраст", "Иван", 30);
НесуществующееПоле = Стр["НеТакоеИмя"];

// Унарный плюс — Число.
ПозитивноеЧисло = +100;
