Процедура Тест()
	Список = Новый Массив();
	Список.Добавить(100);
	Список.Добавить(200);
	Первое = Список[0];
	Второе = Список[1];

	// Цикл по массиву — Element имеет тип element-ref.
	Для Каждого Элемент Из Список Цикл
		ЭлементТип = Элемент;
	КонецЦикла;

	// Структура через withField — Стр.Имя = "X" — accumulateStructureInsertFields.
	Объект = Новый Структура;
	Объект.Вставить("Имя", "Иван");
	Объект.Вставить("Возраст", 30);
	ИмяОбъекта = Объект.Имя;
	ВозрастОбъекта = Объект.Возраст;

	// Соответствие — KV-доступ через []
	Карта = Новый Соответствие;
	Карта.Вставить("К1", 100);
	КлючНайден = Карта["К1"];
КонецПроцедуры
