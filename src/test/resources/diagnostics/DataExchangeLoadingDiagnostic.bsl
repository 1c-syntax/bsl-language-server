Процедура ПередУдалением(Отказ)
    Если ОбменДанными.Загрузка Или ДополнительныеСвойства.Свойство("НеПроверятьУникальность") Тогда
        Возврат;
    КонецЕсли;
    Отказ = ОбщийМодуль.ЕстьОтказ(ОбменДанными.Загрузка);
КонецПроцедуры

Процедура ПередЗаписью(Отказ) // Ошибка
    Если Отказ Тогда
        Сообщить("Это отказ");
    КонецЕсли;
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
    Если ОбменДанными.Загрузка Тогда
        Возврат;
    КонецЕсли;
КонецПроцедуры

Procedure OnWrite(Cancel) // Ошибка
    Var Value;
    If DataExchange.Recipients Then
        Return;
    EndIf;
EndProcedure

Procedure BeforeWrite(Cancel)
    Var Value;
    If DataExchange.Load Then
        Return;
    EndIf;
EndProcedure

Procedure BeforeDelete(Cancel)
    For Each Item in new Array Do
        Return;
    EndDo;

    // Ошибка если ищем только первым
    If DataExchange.Load Then
        Return;
    EndIf;

EndProcedure

Процедура ПриЗаписи(Отказ)
    Если ОбменДанными.Загрузка Тогда
    	Если Не ДополнительныеСвойства.Свойство("ПропуститьОбновлениеВерсииДатЗапретаИзменения") Тогда
    			ДатыЗапретаИзмененияСлужебный.ОбновитьВерсиюДатЗапретаИзмененияПриЗагрузкеДанных(ЭтотОбъект);
    	КонецЕсли;
    	Очистить();
        Возврат;
    КонецЕсли;
КонецПроцедуры



Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)

	Если НЕ ОбменДанными.Загрузка И РольДоступна("ПолныеПрава") И НЕ (ДополнительныеСвойства.Свойство("Обмен") И ДополнительныеСвойства.Обмен = Истина) Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;

КонецПроцедуры

Процедура просто()

КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)

	Если ОбменДанными.Загрузка Тогда
	КонецЕсли;

КонецПроцедуры

