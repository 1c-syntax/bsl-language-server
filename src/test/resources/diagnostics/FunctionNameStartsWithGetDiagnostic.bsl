Функция ПолучитьИмяПоКоду() // Срабатывание

КонецФункции

Функция НеПолучитьИмяПоКоду()

КонецФункции

Функция ИмяПоКоду()

КонецФункции

Процедура ПолучитьИмяПоКоду()

КонецПроцедуры

Function GetNameByCode()

EndFunction

Function NotGetNameByCode()

EndFunction

Function NameByCode()

EndFunction

Procedure GetNameByCode()

EndProcedure
