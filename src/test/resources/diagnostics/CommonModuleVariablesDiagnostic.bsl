// @bsl-module-type=CommonModule

// Предположим, что анализатор считает этот файл общим модулем.

Перем ГлобальнаяПеременная Экспорт; // Ожидаем ошибку здесь
Перем ПростоПеременная; // И здесь ожидаем ошибку

Процедура МояПроцедура()
    Перем ЛокальнаяПеременная; // А вот тут ошибки быть НЕ ДОЛЖНО! Это локальная переменная.
КонецПроцедуры