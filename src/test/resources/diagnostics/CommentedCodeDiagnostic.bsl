// Нормальный комментарий
// Нормальный комментарий: Запрос = Новый Запрос();
// Запрос = Новый Запрос();
// Запрос = 2 + 3;
// ОбщегоНазначения.СвойствоСтруктуры(Стр);
// СтрСоединить(Строки, ", ");

// Процедура Тест()
//   // Тут кааой-то код
// КонецПроцедуры

// Если Услови() Тогда
//   Действие()
// КонецЕсли;