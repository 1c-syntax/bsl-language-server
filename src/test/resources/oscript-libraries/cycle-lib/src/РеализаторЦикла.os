// Реализует интерфейс из циклической иерархии — поиск несуществующего интерфейса
// должен оборваться обходом замыкания, а не зациклиться.

&Реализует("ИнтерфейсЦиклА")
Процедура ПриСозданииОбъекта()
КонецПроцедуры
