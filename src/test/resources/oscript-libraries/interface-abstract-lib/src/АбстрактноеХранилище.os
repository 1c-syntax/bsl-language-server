// Абстрактный класс: объявляет реализацию интерфейса, но сам метод не реализует
// (реализация делегируется наследнику).

&Реализует("ИнтерфейсХранилища")
Процедура ПриСозданииОбъекта()
КонецПроцедуры
