// Конкретный наследник абстрактного класса: фактически реализует интерфейс,
// объявленный родителем (&Реализует на АбстрактноеХранилище).

&Расширяет("АбстрактноеХранилище")
Процедура ПриСозданииОбъекта()
КонецПроцедуры

Процедура Сохранить(Сущность) Экспорт
КонецПроцедуры
