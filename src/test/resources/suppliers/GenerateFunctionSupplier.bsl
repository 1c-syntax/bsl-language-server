
Функция СуществующийМетод(Параметр)
    Возврат Параметр + 1;
КонецФункции

Процедура СуществующаяПроцедура()

КонецПроцедуры

Процедура Инициализация()

    Параметр = СуществующийМетод(1);
    СуществующаяПроцедура();

    НеСуществующийМетод1(Параметр);

    Результат = НеСуществующийМетод1("Строка");

КонецПроцедуры

Инициализация();